import axios from "axios";
import { instance } from "./util/instance";
export const boardWriteApi = async (user) => {
    let boardWriteData = {
        isSuccess: false,
        userId: ""
    }
    // try {
    //     const response = await instance.post("/board", user);
    //     boardWriteData = {
    //         isSuccess: true,
    //         userId: 
    //     }
    // }
}